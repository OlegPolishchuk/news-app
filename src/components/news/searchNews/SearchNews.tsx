import React from 'react';

import { Title } from 'components/title/Title';
import { ReturnComponentType } from 'types';

export const SearchNews = (): ReturnComponentType => {
  return (
    <div>
      <Title title="Searching news" />
    </div>
  );
};
